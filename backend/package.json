{
  "name": "classpolls-backend",
  "version": "1.0.0",
  "description": "Cloudflare Worker Backend for ClassPolls",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "mongodb": "^7.2.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.8",
    "@cloudflare/workers-types": "^4.20240512.0",
    "@types/node": "^25.9.1",
    "typescript": "^5.0.0",
    "vitest": "^4.1.7",
    "wrangler": "^4.93.1"
  }
}
